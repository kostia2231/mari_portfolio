# Cloudflare R2 — только для локального `npm run upload`
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_ENDPOINT=

# Публичный base URL R2 — читается в рантайме (src/lib/cdn.ts).
# На Netlify задаётся в Site settings → Environment variables.
PUBLIC_R2_BASE=
